
Online PC:
With Synaptic download deb files and requorements
sudo dpkg -i <deb files>
sudo apt install <program names>
    (python3
    python3-magic
    python-lzma
    )

    apt-offline



Using Apt-offline:
    files_list = gcc make g++ cmake linux-headers-$(uname -r) dkms linux-image-generic linux-image-extra-virtual linux-source linux-headers-generic gfortran build-essential pkg-config htop libatlas-base-dev libblas-dev liblapack-dev git ffmpeg qtbase5-dev libhdf5-serial-dev python python-dev python-pip

    * sudo apt-offline set apt-offline.sig --install-packages <files_list>  --src-build-dep --update --upgrade
    * Transfer apt-offline.sig to PC with internet connection
    * sudo apt-offline get apt-offline.sig --bundle apt-offline_install_files.zip --threads 8
    * Transfer zip file to Offline PC:
    * sudo apt-offline install /path/to/aptbundle.zip
    * sudo apt install <files_list>





python
python-pip
pip upgrade pip with whl
pip install virtualenv with whl
virtualenv VM
source VM/bin/activate




pscp to transfer files with cmd: IP address is 192.168.33.50


used_python_packages = pyyaml matplotlib numpy scipy pandas tensorflow==1.4.0 h5py keras opencv-contrib-python pillow scikit-learn six tqdm protobuf Pillow pandas-profiling numba jupyter ipdb h5py flask django autopep8



```sudo pip install --download $HOME/pip_install_files pip virtualenv tensorflow==1.4.0 keras scipy numpy pyyaml matplotlib pandas h5py opencv-contrib-python pillow scikit-learn six tqdm protobuf Pillow pandas-profiling numba jupyter ipdb h5py flask django autopep8 ipython scikit-image pytest flask django```

Online PC:
    pip3 install --download $HOME/pip_install_files pip virtualenv  

Offline PC:
    pip3 install --no-index --find-links=file:/$HOME/pip_install_files pip virtualenv --upgrade



pscp
