* use type 'text' for text fields
* use timestampz for dates
* When creating an index, consider if you want a combinatorial index(order matters here) or indices for individual columns, or both
How to use EXPLAIN